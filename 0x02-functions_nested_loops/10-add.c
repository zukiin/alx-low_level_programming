#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: first number input
 * @y: second number input
 * Return: x + y
 */
int add(int x, int y)
{
	return (x + y);
}
